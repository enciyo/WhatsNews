<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools" android:id="@+id/main"
            app:startDestination="@id/topHeadline">

    <fragment android:id="@+id/topHeadline" android:name="com.example.whatsnews.ui.topheadline.TopHeadline"
              android:label="WhatsNews" tools:layout="@layout/top_headline_fragment">
        <action android:id="@+id/toWebView" app:destination="@id/webView"/>
    </fragment>
    <fragment android:id="@+id/webView" android:name="com.example.whatsnews.ui.webview.WebView"
              android:label=" " tools:layout="@layout/web_view_fragment"/>
</navigation>